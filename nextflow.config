profiles{

    standard {
        process.executor = 'local'
    }

    cluster{
        process.executor='lsf'
        process.queue='production-rh74'
        process.clusterOptions = '-R \"select[hname!=\'hx-noah-05-02\']\"'

        executor.queueSize=1000
        executor.perJobMemLimit=true
        executor.exitReadTimeout='100000 sec'
        executor.pollInterval = '5sec'

        conda.cacheDir = "/nfs/production3/ma/home/asolovyev/garnett-workflow/envs" //change this later 
        conda.createTimeout = "30 min"
    } 
}

params{

    matrix = "$baseDir/data/expression_matrix.mtx"
    barcodes = "$baseDir/data/pheno_data.txt"
    genes = "$baseDir/data/feature_data.txt"
    marker_genes = "$baseDir/data/test_marker_file.txt"
    database = "org.Hs.eg.db"
    cds_gene_id_type = "SYMBOL"
    marker_gene_id_type = "SYMBOL"
    classifier_gene_type = "ENSEMBL"
    n_outgroups=50

    cell_id_field = "null"
    predicted_cell_type_field = "cluster_ext_type"

}
